@using Webdiyer.WebControls.Mvc;
@model Webdiyer.WebControls.Mvc.PagedList<ShareModel>
@{
	ViewBag.Title = "资源分享";

	//var Model1 = Model.OrderByDescending(o => o.CreatedTime).Take(5);

	//var Model2 = Model.OrderBy(o => o.CreatedTime).Take(5);

}
<div id="stuning-header">
	<div class="row">
		<div class="twelve columns">
			<div id="page-title">
				<div class="page-title-inner">
					<h1 class="page-title"> @ViewBag.Title </h1>
					<div class="group">
						<div class="block">
							<p>学科高地<br />理事长</p>
							<a href="@Url.Action("Share", new { target = 5002 })" class="btn">查 询</a>
						</div>
						<div class="block">
							<p>学科培训<br />基地主持人</p>
							<a href="@Url.Action("Share", new { target = 5003 })" class="btn">查 询</a>
						</div>
						<div class="block">
							<p>学科培训<br />工作室主持人</p>
							<a href="@Url.Action("Share", new { target = 5004 })" class="btn">查 询</a>
						</div>
						<div class="block">
							<p>学科带头人</p>
							<a href="@Url.Action("Share", new { target = 5005 })" class="btn">查 询</a>
						</div>
						<div class="block">
							<p>骨干教师</p>
							<a href="@Url.Action("Share", new { target = 5006 })" class="btn">查 询</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>


<section id="layout">
	<div class="row">

		<section id="main-content" role="main" class="eight columns">
			@foreach (var item in Model)
			{
				<article class="post type-post status-publish format-standard has-post-thumbnail sticky hentry category-wishes tag-creative tag-gadget">
					@*@if (@item.ThumbUrl != null)
					{
						<div class="post-media">
							<img src="@item.ThumbUrl" />
						</div>
					}*@
					<header>
						<time class="date updated"> <span class="day">@item.CreateDate.ToString("dd")</span> <span class="month">@item.CreateDate.ToString("yyyy-MM")</span> </time>
						<div class="ovh">
							<h3 class="entry-title"><a href="@Url.Action("ShareDetails", "Home", new { id = item.ShareId })" target="_blank">@item.Title</a></h3>
							<div class="entry-meta dopinfo">
								<span class="byline author vcard"><a href="javascript:;" rel="author" class="fn"><strong>@item.RealName</strong></a></span>
							</div>
							<div class="entry-meta dopinfo">
								@Html.Raw(item.Type)
							</div>
							<div class="entry-meta dopinfo">
								共享文件数量：@item.AttachmentCount <a href="@Url.Action("ShareDetails", "Home", new { id = item.ShareId })">【查看详细】</a>
							</div>
							<div>
								<a href="@Url.Action("ShareDetails", "Home", new { id = item.ShareId })" class="link-read-more" target="_blank"></a>
							</div>
						</div>
					</header>
					@*<div class="post-format">
						<i class="icon-pencil-1"></i>
					</div>*@
					@*<div class="entry-content">
						
						
					</div>*@
				</article>
			}
	
			<nav class="page-nav">
				<div class="pagination">
					@Html.Pager(Model).Options(o => o.SetPageIndexParameterName("pageIndex").SetPagerItemTemplate("{0}&nbsp;"))
				</div>
			</nav>

		</section>

		@*<aside class="four columns" id="left-sidebar">
			<section id="crum_widget_tabs-2" class="widget widget_crum_widget_tabs">
				<ul class="tabs contained horisontal">
					<li class="active"><a href="#popular-p-tab">最新新闻</a></li>
					<li><a href="#recent-p-tab">最早新闻</a></li>
				</ul>
				<ul class="tabs-content contained folio-wrap clearfix cl" style="margin: 0 20px;">

					<li id="popular-p-tabTab" class="active">

						@foreach (var item in Model1)
						{
							<article class="hentry mini-news clearfix">
								<div class="entry-thumb">
									<img src="@item.ThumbUrl" />
									<a href="javascript:;" class="link"></a>
								</div>
								<div class="box-name entry-title">
									<a href="@Url.Action("NewsDetail", "Home", new { id = item.NewsId })" target="_blank">@item.Title</a>
								</div>
								<div class="entry-meta dopinfo">
									<span class="byline author vcard"><a href="javascript:;" rel="author" class="fn">@item.RealName</a></span>
									<span class="delim"></span>
									<span class="entry-date updated">@item.CreatedTime.ToString("yyyy/MM/dd")</span>
								</div>
							</article>
						}

					</li>

					<li id="recent-p-tabTab">
						@foreach (var item in Model2)
						{
							<article class="hentry mini-news clearfix">
								<div class="entry-thumb">
									<img src="@item.ThumbUrl" />
									<a href="javascript:;" class="link"></a>
								</div>
								<div class="box-name entry-title">
									<a href="@Url.Action("NewsDetail", "Home", new { id = item.NewsId })" target="_blank">@item.Title</a>
								</div>
								<div class="entry-meta dopinfo">
									<span class="byline author vcard"><a href="javascript:;" rel="author" class="fn">@item.RealName</a></span>
									<span class="delim"></span>
									<span class="entry-date updated">@item.CreatedTime.ToString("yyyy/MM/dd")</span>
								</div>
							</article>
						}
						
					</li>
				</ul>
			</section>
		</aside>*@

	</div>
</section>

@section plugins{
	@Scripts.Render("~/js/app_foundation")
}

@section scripts{
	<script>

		$(function () {
			$(".entry-content .content").each(function (index, item) {
				var sub = $(this).text();
				sub = sub.length > 90 ? sub.substr(0, 87) + ' ...' : sub;
				$(this).html(sub);
			});
		});

	</script>
}