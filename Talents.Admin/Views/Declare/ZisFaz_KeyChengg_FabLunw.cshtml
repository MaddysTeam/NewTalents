@model List<ZisFaz_KeyChengg_FabLunwModel>
@{ 
	var pageTitle = ViewBag.Title = DeclareKeys.ZisFaz_KeyChengg_FabLunw;
}
<!-- WIDGET TABBED CONTENT -->
<div class="widget">
	<div class="widget-header">
		<h3><i class="fa fa-paperclip"></i> @pageTitle</h3>
		<div class="btn-group widget-header-toolbar">
			<a href="javascript:;" title="焦点" class="btn-borderless btn-focus"><i class="fa fa-eye"></i></a>
			<a href="javascript:;" title="全屏" class="btn-borderless btn-expand"><i class="fa fa-expand"></i></a>
		</div>
	</div>
	<div class="widget-content">

		<div class="table-responsive">
			<table class="table table-condensed table-striped table-hover">
				<thead>
					<tr>
						<th>级别</th>
						<th>发表时间</th>
						<th>论文名称</th>
						<th>作者名次</th>
						<th>刊物名称</th>
						<th class="width240">操作</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var item in Model)
					{
						<tr>
							<td>@item.Level</td>
							<td>@item.Date.ToString("yyyy-MM-dd")</td>
							<td>@item.NameOrTitle</td>
							<td>@item.Dynamic2</td>
							<td>@item.Dynamic1</td>
							<td>
								<div class="btn-group btn-group-xs pull-right">
									@if (item.IsShare)
									{
										<button class="btn btn-custom-secondary btn-share" data-url="@Url.Action("ShareAchievement", "Declare", new { id = item.DeclareAchievementId, isShare = false })">取消共享</button>
									}
									else
									{
										<button class="btn btn-custom-secondary btn-share" data-url="@Url.Action("ShareAchievement", "Declare", new { id = item.DeclareAchievementId, isShare = true })">共享</button>
									}
									<button class="btn btn-danger btn-view" data-toggle="ajax-modal" data-url="@Url.Action("AttachmentsView", "DeclarePop", new { id = item.DeclareAchievementId, type = AttachmentsKeys.ZisFaz_KeyChengg_FabLunw })" data-target="#firstModal" title="查看附件">查看附件</button>
									<button class="btn btn-success btn-edit" data-toggle="ajax-modal" data-url="@Url.Action("ZisFaz_KeyChengg_FabLunw", "DeclarePop", new { id = item.DeclareAchievementId })" data-target="#firstModal" title="编辑">编辑</button>
									<button class="btn btn-warning btn-remove" data-url="@Url.Action("RemoveAchievement", "Declare", new { id = item.DeclareAchievementId, type = AttachmentsKeys.ZisFaz_KeyChengg_FabLunw })" title="删除">删除</button>
								</div>
							</td>
						</tr>
					}
				</tbody>
				<tfoot>
					<tr>
						<td colspan="5"></td>
						<td>
							<div class="btn-group btn-group-xs pull-right">
								<button class="btn btn-custom-primary btn-add" data-toggle="ajax-modal" data-url="@Url.Action("ZisFaz_KeyChengg_FabLunw", "DeclarePop")" data-target="#firstModal"><i class="fa fa-plus-square"></i> <span>添加</span></button>
							</div>
						</td>
					</tr>
				</tfoot>
			</table>
		</div>

	</div>
</div>
<!-- END WIDGET TABBED CONTENT -->
@Html.Partial("../Utilities/_partialJS")
